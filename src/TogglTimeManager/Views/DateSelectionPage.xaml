<Page x:Class="TogglTimeManager.Views.DateSelectionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewModels="clr-namespace:TogglTimeManager.ViewModels"
      xmlns:System="clr-namespace:System;assembly=mscorlib"
      xmlns:local="clr-namespace:TogglTimeManager.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      d:DataContext="{d:DesignInstance viewModels:DateSelectionViewModel}"
      Title="DateSelectionPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0">
            <Label Content="What is the period of the csv?" FontWeight="Bold"></Label>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <Label Content="From"/>
                <DatePicker SelectedDate="{Binding From, TargetNullValue={x:Static System:DateTime.Now}}"/>
                <Label Content="to"/>
                <DatePicker SelectedDate="{Binding To, TargetNullValue={x:Static System:DateTime.Now}}"/>
            </StackPanel>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="1">
            <Button HorizontalAlignment="Left" Width="60" Margin="0,10,20,10" Command="{Binding GoBackCommand}">Previous</Button>
            <Button HorizontalAlignment="Left" Width="60" Margin="20,10,0,10" Command="{Binding NextCommand}">Next</Button>
        </StackPanel>
    </Grid>
</Page>
